<UserControl x:Class="Engine.Views.Pages.StoriesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:Engine.Views.Controls"
             xmlns:m="clr-namespace:Engine.Models">
    <Grid Background="{DynamicResource PrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Истории" Style="{StaticResource TitleText}" Margin="32,28,32,8"/>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                      Margin="20,8,20,16">
            <ItemsControl ItemsSource="{Binding Stories}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type m:Story}">
                        <Button Background="Transparent" BorderThickness="0" Padding="0" Cursor="Hand"
                                Command="{Binding DataContext.ToggleStoryCommand,
                                         RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Button.Template>
                            <c:StoryCircle/>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <ScrollViewer Grid.Row="2" Padding="32,0,32,32">
            <ItemsControl ItemsSource="{Binding Stories}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type m:Story}">
                        <Button Background="Transparent" BorderThickness="0" Padding="0" Margin="6" Cursor="Hand"
                                Command="{Binding DataContext.ToggleStoryCommand,
                                         RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Height="180" CornerRadius="14" ClipToBounds="True">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="{Binding GradientStart, Converter={StaticResource HexToBrush}}" Offset="0"/>
                                                <GradientStop Color="{Binding GradientEnd, Converter={StaticResource HexToBrush}}" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <Grid>
                                            <TextBlock Text="{Binding PlaceholderText}"
                                                       Foreground="#33FFFFFF"
                                                       FontFamily="{StaticResource UiFontSemibold}"
                                                       FontSize="68"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                            <StackPanel VerticalAlignment="Bottom" Margin="14">
                                                <TextBlock Text="{Binding Author.FullName}"
                                                           Foreground="White"
                                                           FontFamily="{StaticResource UiFontSemibold}"/>
                                                <TextBlock Text="{Binding TimeAgo}"
                                                           Foreground="#CCFFFFFF"
                                                           FontSize="11"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>