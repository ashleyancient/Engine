<UserControl x:Class="Engine.Views.Pages.FavoritesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource PrimaryBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="520">
            <Border Width="120" Height="120" CornerRadius="60"
                    Background="{DynamicResource AccentGradientBrush}">
                <TextBlock Text="*"
                           FontFamily="{StaticResource UiFontSemibold}"
                           FontSize="64"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
            <TextBlock Text="Избранное"
                       Style="{StaticResource TitleText}"
                       HorizontalAlignment="Center"
                       Margin="0,18,0,4"/>
            <TextBlock Text="Здесь хранятся ваши важные сообщения, ссылки и заметки. Используйте этот раздел как личный блокнот в Engine."
                       Style="{StaticResource SecondaryText}"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,0,0,18"/>
            <Button Content="Открыть избранное"
                    Style="{StaticResource AccentButton}"
                    Command="{Binding NavigateCommand}"
                    CommandParameter="Chats"
                    HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>