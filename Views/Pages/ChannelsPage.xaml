<UserControl x:Class="Engine.Views.Pages.ChannelsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:Engine.Views.Controls"
             xmlns:m="clr-namespace:Engine.Models">
    <Grid Background="{DynamicResource PrimaryBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="32,28,32,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Каналы" Style="{StaticResource TitleText}"/>
            <Border Grid.Column="1" Width="280" Background="{DynamicResource CardBrush}" CornerRadius="22"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <TextBox Style="{StaticResource SearchBox}"
                         Background="Transparent"
                         BorderThickness="0"/>
            </Border>
        </Grid>

        <ScrollViewer Grid.Row="2" Padding="32,8,32,32">
            <StackPanel>
                <TextBlock Text="Мои каналы" Style="{StaticResource HeaderText}" Margin="0,0,0,12"/>
                <ItemsControl ItemsSource="{Binding Channels}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type m:Channel}">
                            <Border Style="{StaticResource Card}" Margin="0,4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <c:AvatarControl Size="56"
                                                     Initials="{Binding Initials}"
                                                     ColorStart="{Binding AvatarColorStart}"
                                                     ColorEnd="{Binding AvatarColorEnd}"
                                                     Margin="0,0,16,0"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Title}"
                                                       FontFamily="{StaticResource UiFontSemibold}"
                                                       FontSize="15"/>
                                            <TextBlock Text="{Binding Username}"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       Margin="8,0,0,0"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Description}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   TextWrapping="Wrap"
                                                   MaxHeight="36"
                                                   TextTrimming="CharacterEllipsis"
                                                   Margin="0,4,0,0"/>
                                        <TextBlock Text="{Binding SubscribersText}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource AccentSecondaryBrush}"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>
                                    <Button Grid.Column="2"
                                            VerticalAlignment="Center"
                                            Padding="18,8"
                                            FontFamily="{StaticResource UiFontSemibold}"
                                            Content="{Binding SubscribeButtonText}"
                                            Command="{Binding DataContext.SubscribeChannelCommand,
                                                     RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource BaseButton}">
                                                <Setter Property="Background" Value="{DynamicResource AccentGradientBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSubscribed}" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>